[package]
name = "alchemy"
version = "0.1.0"
authors = ["Lain <lain@lain.org.uk>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "1.0.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"